import SearchBar from "./SearchBar";

// ez tartalmazza a nav bárt + search bart
const NavBar = () => {
  return (
    <div>
      navbárom benne searchbar
      <SearchBar />
    </div>
  );
};

export default NavBar;
